"""Stock video provider package (Pexels, Pixabay).

Each provider searches a free stock-video API for vertical (portrait)
clips used as render backgrounds. Without an API key, providers fall
back to a small set of curated demo clips so the studio flow works
end-to-end offline.
"""
