# --- Core API -------------------------------------------------------------
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
python-dotenv>=1.0
python-multipart>=0.0.9

# --- HTTP client (LLM / TTS providers) -------------------------------------
httpx>=0.27

# --- TTS ---------------------------------------------------------------------
# Free Azure neural voices via Microsoft Edge's TTS service (default provider)
edge-tts>=6.1.9

# --- Rendering ---------------------------------------------------------------
# Reddit-style card rendering. Video composition itself shells out to the
# `ffmpeg` BINARY (must be on PATH: `brew install ffmpeg` / `apt install ffmpeg`).
Pillow>=10.3
# moviepy>=2.0  # optional alternative wrapper for complex effects

# --- Dev / test ---------------------------------------------------------------
pytest>=8.0
pytest-asyncio>=0.23
